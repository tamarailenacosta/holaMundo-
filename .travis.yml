language: node_js
services: docker
install: skip
jobs:
   include:
   - stage: Debricked-scan
     on:
       branch: master
     script: docker run
       -v $HOME:$HOME -e debricked/debricked-scan:latest DEBRICKED_TOKEN=${DEBRICKED_TOKEN} REPOSITORY=${TRAVIS_REPO_SLUG} COMMIT=${TRAVIS_COMMIT} 
      # docker run -it --rm -v $PWD:/data debricked/debricked-cli debricked:scan "" ${DEBRICKED_TOKEN} ${REPOSITORY} ${COMMIT} null cli

      
