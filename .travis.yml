jobs:
   include:
   - stage: Debricked
     #install: skip
     env:
      -  USERNAME: ""
      -  DEBRICKED_TOKEN: $(DEBRICKED_TOKEN)
      -  "TRAVIS_REPO_SLUG"
      -  "TRAVIS_COMMIT"
      -  "null"
      -  "cli"
     script: env > env.list |
        docker run -it --rm -v  -e ./env.list $PWD:/data debricked:scan
        
      
