jobs:
   include:
   - stage: Debricked
     #install: skip
     env:
      -  USERNAME: ""
      -  DEBRICKED_TOKEN: $(DEBRICKED_TOKEN)
      -  "TRAVIS_REPO_SLUG"
      -  "TRAVIS_COMMIT"
      -  "null"
      -  "cli"
     script: env > env.list |
        docker run -v $HOME:$HOME --env-file ./env.list $PWD:/data debricked:scan
        
      
