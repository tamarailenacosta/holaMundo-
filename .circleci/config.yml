version: 2.1

jobs:
  debricked-scan:
    docker:
      - image: debricked/cli
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run: debricked scan

workflows:
  debricked:
    jobs:
      - debricked-scan
