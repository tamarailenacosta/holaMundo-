language: node_js
node_js: 
  - "12"
services:
  - docker
before_install:
- docker pull debricked/debricked-cli
script:
  - docker run -it --rm -v $PWD:/data debricked/debricked-cli debricked:scan USERNAME="" ${DEBRICKED_TOKEN} ${REPOSITORY}  ${COMMIT} null cli


