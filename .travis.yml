language: node_js
services: docker
install: skip
jobs:
    include:
        - stage: Debricked-scan
          on:
              branch: master
          script:
            docker run -it --rm -v $PWD:/data --entrypoint "/travis-ci.sh" viktigpetterr/travis-scan "" ${DEBRICKED_TOKEN} REPOSITORY=${TRAVIS_REPO_SLUG} 
             COMMIT=${TRAVIS_COMMIT}
          
      
