language: node_js
services: docker
#install: skip
jobs:
    include:
        - stage: Debricked-scan
          on:
            branch: master
          before_install: env > .env
          #script: docker run -e DEBRICKED_TOKEN=${DEBRICKED_TOKEN} -e TRAVIS_REPO_SLUG=${REPOSITORY} -e TRAVIS_BRANCH="master"
            -e TRAVIS_COMMIT=${COMMIT} -e TRAVIS_BUILD_DIR="/repo" -v $(pwd):/repo --entrypoint "/travis-ci.sh" viktigpetterr/travis-scan
          script: docker run -it --env-file .env -v $(pwd):/repo --entrypoint "/travis-ci.sh" viktigpetterr/travis-scan
          
